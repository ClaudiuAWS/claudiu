export default function LoadingSpinner() {   return (     <div       className="fixed inset-0 z-50 flex flex-col items-center justify-center gap-8"       style={{ background: 'linear-gradient(180deg, #0a0f1a 0%, #060a12 100%)' }}     >       {/* Equalizer bars */}       <div className="flex items-end gap-1.5" style={{ height: 48 }}>         {[32, 44, 48, 40, 28].map((h, i) => (           <div             key={i}             className="loading-bar w-2 rounded-full"             style={{               height: h,               background: 'linear-gradient(to top, #dc2626, #f87171)',             }}           />         ))}       </div>        {/* Label */}       <div className="text-center">         <p className="text-white/80 text-sm font-semibold tracking-[0.2em] uppercase">           Loading         </p>         <p className="font-stadium text-white/30 text-[12px] tracking-widest uppercase mt-1">           Bundesliga         </p>         <p className="font-stadium text-white/30 text-[12px] tracking-widest uppercase">           Brezn         </p>       </div>     </div>   ) }